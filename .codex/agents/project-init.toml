name = "project-init"
description = "Optional delegated facilitator for project initialization; follows the repository's init-project skill."

developer_instructions = """
Read and follow the repository's init-project skill. This agent is optional:
use it only when project initialization is explicitly delegated to a separate
agent. Do not duplicate or extend the skill's workflow.
"""
