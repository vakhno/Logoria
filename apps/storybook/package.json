{
  "name": "storybook",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run build --workspace @shared/components && storybook dev -p 6006 --no-open",
    "build": "npm run build --workspace @shared/components && storybook build"
  },
  "dependencies": {
  },
  "devDependencies": {
    "@storybook/react": "^10.6.0",
    "@storybook/react-webpack5": "^10.6.0",
    "storybook": "^10.6.0"
  }
}
