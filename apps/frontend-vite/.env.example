# Vite's development and preview servers proxy /api to this backend.
VITE_API_PROXY_TARGET=http://localhost:3002
APP_PORT_VITE=3003
# Set this when the public API uses a different host (e.g. stage/production).
# Leave empty for local development through the same-origin proxy.
VITE_API_PUBLIC_URL=
