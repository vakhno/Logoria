{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build --workspace @shared/routes && npm run build --workspace @shared/auth && npm run build --workspace @shared/db && nest build && cross-env TSC_WATCHFILE=PriorityPollingInterval TSC_WATCHDIRECTORY=RecursiveDirectoryUsingFsWatchFile nest start --watch",
    "dev:3002": "npm run build --workspace @shared/routes && npm run build --workspace @shared/auth && npm run build --workspace @shared/db && nest build && cross-env API_PORT=3002 TSC_WATCHFILE=PriorityPollingInterval TSC_WATCHDIRECTORY=RecursiveDirectoryUsingFsWatchFile nest start --watch",
    "build": "npm run build --workspace @shared/routes && npm run build --workspace @shared/auth && npm run build --workspace @shared/db && nest build",
    "serve:container": "node dist/main.js",
    "start": "node dist/main.js",
    "lint": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@nestjs/common": "^12.0.1",
    "@nestjs/core": "^12.0.1",
    "@nestjs/platform-express": "^12.0.1",
    "@shared/auth": "file:../../shared/auth",
    "@shared/db": "file:../../shared/db",
    "@shared/routes": "file:../../shared/routes",
    "better-auth": "^1.7.2",
    "cross-env": "^10.1.0",
    "dotenv": "^17.4.2",
    "drizzle-orm": "^0.45.2",
    "express": "^5.2.1",
    "pg": "^8.23.0",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2"
  },
  "devDependencies": {
    "@nestjs/cli": "^12.0.0",
    "@types/express": "^5.0.5",
    "@types/node": "^26.4.1",
    "@types/pg": "^8.23.1",
    "typescript": "^6.0.3"
  }
}
